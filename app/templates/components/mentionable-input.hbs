<PaperInput
        @textarea={{true}}
        @block={{true}}
        @label={{this.label}}
        @maxlength={{1000}}
        @passThru={{hash maxRows=4}}
        @required={{@required}}
        @value={{@value}}
        @onChange={{fn this.onTextChange}}
        @spellcheck={{false}}
        @onKeyDown={{fn this.keyDown}}
        {{did-insert this.setTextAreaWrapperElement}}>
  {{#if this.showHint}}
    <div class="hint">
      Type "{{this.specialCharacter}}" to mention
    </div>
  {{/if}}
  <StyledInputText
          @commentText={{@value}}
          @existingMentions={{this.alreadyMentioned}}
          @mentionPattern={{this.mentionPattern}}
          @textAreaElement={{this.textAreaElement}}>
  </StyledInputText>
  {{#if (and this.currentMention this.enableMentions)}}
    <MentionOptions @currentMention={{this.currentMention}}
                    @options={{@mentionableOptions}}
                    @hideOptions={{fn this.closeOptionsDropdown}}
                    @doMention={{fn this.doMention}}
                    @textAreaElement={{this.textAreaElement}}>
    </MentionOptions>
  {{/if}}
</PaperInput>
